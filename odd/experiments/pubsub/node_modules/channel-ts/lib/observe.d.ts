import { SimpleReceiver } from "./";
interface Observer<T> {
    notify: () => void;
    receiver: () => SimpleReceiver<T>;
    removeReceiver: (chan: SimpleReceiver<T>) => void;
}
export declare type Observable<T> = T & Observer<T>;
export declare const observe: <T extends {}>(t: T) => Observable<T>;
export {};
