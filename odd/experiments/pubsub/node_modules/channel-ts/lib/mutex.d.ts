declare type ResolveList = ((guard: Guard) => void)[];
export declare class Mutex {
    private resolveList;
    private locked;
    constructor();
    acquire(): Promise<Guard>;
}
export declare class Guard {
    private resolveList;
    private locked;
    constructor(lockList: ResolveList, locked: [boolean]);
    release(): void;
}
export {};
