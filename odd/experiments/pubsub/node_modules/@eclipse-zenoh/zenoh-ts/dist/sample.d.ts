import { KeyExpr } from "./key_expr.js";
import { ZBytes } from "./z_bytes.js";
import { Encoding } from "./encoding.js";
import { CongestionControl, Priority, SampleKind } from "./enums.js";
import { Timestamp } from "./timestamp.js";
export declare class Sample {
    private readonly keyexpr_;
    private readonly payload_;
    private readonly kind_;
    private readonly encoding_;
    private readonly attachment_;
    private readonly timestamp_;
    private readonly priority_;
    private readonly congestionControl_;
    private readonly express_;
    /**
     * @internal
     */
    constructor(keyexpr_: KeyExpr, payload_: ZBytes, kind_: SampleKind, encoding_: Encoding, attachment_: ZBytes | undefined, timestamp_: Timestamp | undefined, priority_: Priority, congestionControl_: CongestionControl, express_: boolean);
    keyexpr(): KeyExpr;
    payload(): ZBytes;
    kind(): SampleKind;
    encoding(): Encoding;
    timestamp(): Timestamp | undefined;
    congestionControl(): CongestionControl;
    priority(): Priority;
    express(): boolean;
    attachment(): ZBytes | undefined;
}
