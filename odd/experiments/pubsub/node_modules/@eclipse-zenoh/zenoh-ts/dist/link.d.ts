export declare class RemoteLink {
    private ws;
    private constructor();
    static new(locator: string): Promise<RemoteLink>;
    onmessage(onmessage: (msg: Uint8Array) => void): void;
    send(msg: Uint8Array): Promise<void>;
    isOk(): boolean;
    close(): Promise<void>;
    private static parseZenohLocator;
}
